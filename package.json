{
  "name": "healico-site-rules",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "node scripts/prepare-wiki.cjs && vitepress dev docs",
    "docs:build": "node scripts/prepare-wiki.cjs && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test": "node scripts/test-rule-blog.cjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
